export interface IPersonWebFormConfig {
  title: string;
  nameLabel: string;
  nicknameLabel: string;
  genderLabel?: string;
  dateLabel?: string;
  federalLabel: string;
  stateLabel: string;
  bondLabel?: string;
}

export interface IPersonWebListConfig {
  title: string;
}

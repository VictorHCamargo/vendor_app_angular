import { TypeAddressNamePipe } from './type-address-name-pipe';

describe('TypeAddressNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeAddressNamePipe();
    expect(pipe).toBeTruthy();
  });
});
